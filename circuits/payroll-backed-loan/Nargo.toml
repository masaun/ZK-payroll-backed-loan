[package]
name = "payroll_backed_loan"
type = "bin"
authors = [""]

[dependencies]
# Poseidon hash function for Noir
poseidon = { tag = "v0.1.1", git = "https://github.com/noir-lang/poseidon" }

# OFAC compliance check library
sanction_checker = { path = "../sanction_checker" }