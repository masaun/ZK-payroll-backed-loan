pub struct PublicOutputs {
    pub nullifier: Field    // @dev - Nullifier to prevent double-spending
    // loan_approved: bool,  // @dev - Indicates if the loan is approved
    // max_loan_amount: u64, // @dev - The maximum loan amount the borrower is eligible for
}